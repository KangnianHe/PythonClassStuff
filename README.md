# multiplayer example

a simple multiplayer 3d demo using flask + socket.io.

## setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## run

```bash
python server.py
```

open `http://localhost:5000` in multiple browser tabs to see players move around together.
